# Old C device files removed - all devices now use C++ implementations (_v2.hpp files)
# The C++ device registry (device_registry.cpp) registers all devices
# No need to list individual device files here anymore
set(SOURCE_FILES ${SOURCE_FILES}
    PARENT_SCOPE)
